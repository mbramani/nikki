import tw, { styled } from 'twin.macro'
import { Container as MainContainer } from '../login/LoginStyles'

export const Container = styled(MainContainer)`
  ${tw`flex items-center justify-center h-screen`}
`
